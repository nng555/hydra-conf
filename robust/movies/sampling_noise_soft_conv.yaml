train:
  warmup_updates: 3000
  max_epoch: 15
  soft_labels: true
  save_interval_updates: 200
  keep_interval_updates: 1

gen:
  model:
    date: 2020-05-04
    bin: orig
    seed: 0

extra: conv

data:
  bin: sampling_${gen.mask_prob}_soft_conv
