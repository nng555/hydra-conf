train:
  warmup_updates: 2000
  max_epoch: 15
  num_classes: 2

data:
  bin: sampling_acc_${data.accuracy}_hard
  accuracy: ~

eval:
  model:
    date: ~

display:
  dir: 
    date: ~

gen:
  model:
    date: 2020-04-23
    bin: orig_acc_${data.accuracy}
    seed: 0
